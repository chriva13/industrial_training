import 'package:flutter/material.dart';

class BoomColorChange extends StatelessWidget {
  const BoomColorChange({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Boom Color Change'),
      ),
    );
  }
}
